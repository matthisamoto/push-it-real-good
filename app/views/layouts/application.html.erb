<!DOCTYPE html>
<html>
<head>
  <title>Push It Real Good -- Create your own one-button-site.</title>
  <%= csrf_meta_tags %>
  <link rel="stylesheet" href="<%= root_path %>stylesheets/base.css" type="text/css" />
  <link rel="stylesheet" href="<%= root_path %>stylesheets/main.css" type="text/css" />
  <link rel="stylesheet" href="<%= root_path %>stylesheets/sc-player-minimal.css" type="text/css" />
  <link href='http://fonts.googleapis.com/css?family=Bowlby+One' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js" type="text/javascript"></script>
</head>
<body>

<div class="wrapper">
  <div class="nav_bar clearfix">
    <div class="session_status">
      <% if user_signed_in? %>
        Logged in as <%= current_user.username %>. Go to my <%= link_to "profile", "/user/#{current_user.username}" %> | Not you?
        <%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %>
      <% else %>
        <%= link_to "Login", new_user_session_path %> or <%= link_to "Register", new_user_registration_path %>
      <% end %>
    </div>
  </div>
  
  <div class="content">

    <%= yield %>

  </div> <!-- content -->

</div> <!-- wrapper -->

<footer>
  <nav>
    <ul>
      <li><%= link_to "What is PIRG?", "/wtf" %></li>
      <li><%= link_to "Legal Terms", "/legal" %></li>
      <li><%= link_to "Contact Us", "mailto:pusha@pushitrealgood.com" %></li>
      <li>&#169; 2011 PUSHITREALGOOD</li>
    </ul>
  </nav>
</footer>

</body>
</html>
